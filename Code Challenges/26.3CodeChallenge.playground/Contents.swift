import UIKit

struct Position {
    var x: Int
    var y: Int
}

func robotRoam(array: [Int]) -> [Position] {
    let currentDegree = [0]
    if array.count >= 0 {
        let North = [0]
        let East = [90]
        let South = [180]
        let West = [270]
        
    switch array {
    case North:
    case East:
        break
    case South:
        break
    case West:
        break
    default:
        return (Position.x, Position.y)
        
        }
    }
}

